import { ChannelsSelectElement } from '@rocket.chat/ui-kit';

/**
 * Parameters for creating a ChannelsSelectElement.
 * This type includes specific properties from the original ChannelsSelectElement type.
 */
export type ChannelSelectParam = Pick<ChannelsSelectElement, 'appId'>;
